using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class batComponentScript : MonoBehaviour
{
    // Start is called before the first frame update
    public void batDestroy()
	{
		Destroy(gameObject);
	}
}
